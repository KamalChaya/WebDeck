
function mk_network()
{
	this.game_id
	
	//Member functions
	update_board()//Get x,y positions and flip value
	set_pos(card_id)//set the x,y position
	select()//
	secure_lock()//
	flipdb(card_id, flip_val) //Sets "flipped" to the new flip_val
}